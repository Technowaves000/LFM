{{#ifEquals var value=value }}
<input type="radio" class="{{class}}" name="{{name}}" value="{{value}}" id="{{id}}" checked>
{{else}}
<input type="radio" class="{{class}}" name="{{name}}" value="{{value}}" id="{{id}}">
{{/ifEquals}}
